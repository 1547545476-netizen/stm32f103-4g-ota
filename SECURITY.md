# 凭据与安全边界

## 本仓库不需要AI账号

该工程的构建和固件通信不调用OpenAI API，也不依赖ChatGPT/Codex登录。公开C/Python源码不会授权他人使用开发者的AI账号。本仓库不应包含AI配置、OAuth登录文件、API Key、浏览器Cookie、会话导出或Git凭据。

## 必须留在本机的内容

- `User/OtaSecrets.h`：MQTT账号密码、设备标识和OTA HMAC密钥。
- `.env`、`auth.json`、`credentials.json`、`.codex/`及其他本机登录配置。
- 私钥、令牌、带访问签名的URL、原始串口/调试日志。
- 所有预编译固件和中间产物：Boot内含HMAC密钥，APP可能包含MQTT凭据。
- 开发目录原有的`.git`历史，及临时下载、备份、厂商PDF和调试器状态。

配置模板只有占位值，公开算法和固定测试向量不是泄露设备秘密。生成的每设备密钥与测试向量必须区分。

## 检查方式

`tools/ci/check_public_tree.py`按禁止文件名、常见令牌格式、私钥头、非占位凭据宏、带签名URL和本机绝对路径进行检查，不输出命中的秘密原文。完整扫描适用于纯净发布目录；`--tracked`逐项读取Git索引内容，检查真正准备提交的版本。

扫描是辅助措施，无法保证识别所有自定义编码或人为隐藏的秘密。若秘密曾公开，应在对应服务撤销/轮换；仅删除最新文件或添加.gitignore不能删除旧Git提交中的内容。不要将真实凭据贴到公开Issue讨论问题。

## 固件安全限制

CRC不能验证发布者身份。Boot使用HMAC-SHA256认证固件，但密钥位于MCU Flash，未宣称已经实施安全元件、RDP/写保护量产策略或非对称签名。公开Broker不具备该项目专属Topic授权；部署需要自行配置私有Broker、设备权限、CA和密钥管理。

发布工具要求HTTPS URL；仅凭URL以https开头不等于所有Air780E固件版本均正确验证证书，应结合模块配置和实测确认。公共读对象存储允许任何人获取文件；需要保密的固件应另外设计受控分发，认证本身不加密固件。
