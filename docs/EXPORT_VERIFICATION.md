# 源码发布副本验证

日期：2026-09-11。

验证在独立副本中进行，使用配置模板生成一次性HMAC密钥。没有使用真实MQTT账号，没有连接云端、发送升级命令或烧录设备。

| 项目 | 结果 |
| --- | --- |
| Boot ARMCC 5.06全量构建 | 0 Error / 0 Warning；Code 16852、RO 700、RW 28、ZI 5060字节 |
| APP ARMCC 5.06全量构建 | 0 Error / 0 Warning；bin 20216字节 |
| APP向量 | MSP `0x20000E18`，Reset_Handler `0x08005105` |
| 分区与W25Q64槽 | 通过源码一致性检查 |
| APP HMAC密钥字符串隔离检查 | 通过；不等同于硬件隔离证明 |
| 发布侧HMAC固定向量 | 通过 |
| 敏感文件扫描器回归 | 4项通过，包括读取Git索引而非仅检查工作区 |

APP尺寸会受本机配置字符串和编译选项影响，此结果只对应本次模板构建。发布目录不含上述临时配置、二进制、日志和验证仓库。

发布副本适配：移除真实OSS测试URL和设备Topic默认值；构建脚本使用KEIL_ROOT；4个厂商源文件的西文编码转换为UTF-8，版权原文保留。VS Code任务与必要文档重新整理，工程核心行为未在本次发布打包中重构。
